import React from 'react';
import { Form, Input } from 'reactstrap';

const CreateAccount = () => {
    return (
        <div>
            <Form>
                Input
            </Form>
        </div>
    );
};

export default CreateAccount;